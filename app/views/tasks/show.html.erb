<p><%= @task.title %></p>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "Delete", task_path(@task), data: { "turbo-method": :delete }, class:"btn btn-danger" %>
<%= link_to "edit task", edit_task_path(@task), class: "btn btn-warning"%>
